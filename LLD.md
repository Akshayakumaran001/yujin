# Low-Level Design

## Modules
Auth, User, Contact, Message, Template, Queue, Worker, WhatsApp, Logs

## Worker Loop
while(queue):
  wait(random delay)
  send
  log

## Template Engine
{{name}} replacement
